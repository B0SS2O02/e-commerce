<div class="container">
    <nav class="vertical-nav">
        <div class="logo">
            <img src="/public/logo.png" alt="Logo">
        </div>
        <ul>
            <% navbar.forEach(nav=> { %>
                <li>
                    <a href="<%= nav.link %>">
                        <%= nav.title %>
                    </a>
                </li>
                <%})%>

        </ul>
    </nav>
</div>